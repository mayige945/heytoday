# AGENTS.md

This file provides guidance to Codex (Codex.ai/code) when working with code in this repository.

## 这是什么仓库

「喂，今天」是一档给 9–15 岁孩子的**每日新闻播客**的产品/内容仓库。**目前没有代码、没有构建系统、没有测试**——只有三份 Markdown 规格文档。当前阶段的真实产出是**手写的播客稿子**（N=1，作者本人的孩子听），通过"出稿 → 看孩子反应 → 改 prompt → 次日再看"的高频循环，逼近"什么是好稿"。

因此：不要假设存在 build/lint/test 命令；不要去搭脚手架，除非用户明确要求开始做工程实现。这里的"开发"主要是**写文档、写/改稿、迭代 Writing_Kit 里的 prompt**。

## 文档结构与依赖关系

三份文档不是平级的，有明确的上下游：

- **`PRD_v0.6.md`** — 唯一的真理来源（source of truth）。定义了产品的魂：两层问题、**五条核心机制**、北极星（思维+性格，v1 不执行不考核）、**七条产品宪法**、引擎五层、MVP 计划。任何其它文档和任何稿子都不得与它冲突。改动产品决策**只在这里改**。
- **`Content_Safety_Policy_v0.1.md`** — PRD 第七节的执行细则。把"红线 / 敏感但重要 / 默认可聊"三层和家长三档开关填实到能用。明确声明**不动主线、不改任何已定设定、不给家长加新开关**。判定逻辑恒为 `家长档 × 年龄档`。
- **`Writing_Kit.md`** — 把 PRD 的魂压缩成一份**可直接粘进新对话当开场白**的写稿 prompt。每天用法："话题：[新闻]；档：[小学/初中]"。这是 PRD 第三节那套机制如何落进 prompt 的当前快照，也是 Narrative 引擎的雏形。

迭代方向：用 MVP 真实运行中的信号（**家长否决** + **AI 标"不确定"**）回流校准 → 校准 `Content_Safety_Policy` 的原则/红线，沉淀 `Writing_Kit` 的 prompt 规则。

## 当前在跑什么（MVP 运行态）

- **档位已锁定「小学高年级」**先跑通（用户 2026-06-29 决定）。初中档暂不写。
- **`prompts/`** — 正在反复迭代的写稿 prompt，按档位+版本命名（当前：`小学高年级_写稿提示词_v0.9.md`）。这是 `Writing_Kit.md` 收窄到单档、可迭代的运行版，**是当前出稿的真正依据**。每改一次升小版本、在文件内「迭代记录」写清改了什么和为什么。改 prompt 是这阶段最重要的工作（方法论第六步）。
- **`稿子/`** — 每天产出的单期文稿，命名 `日期_话题_档.md`，含元数据 + 选题记录 + 出稿自检 + 风险提示。
- **`手工跑通/`** — 尚未工程化、但已经进入重复验证的手工任务目录。每个编号子目录就是一个可执行任务，含目标、输入、步骤、产物、通过标准和沉淀位置；音频导演、TTS 生成、评估复听等过程文档放这里，不散落到项目根目录或 `探索/`。
- **每日手工主线**（方法论第六步「先用手跑通，别先造系统」）：① 出 3 候选、标 1 推荐，家长选 1（PRD §7）→ ② 按 `prompts/` 当前版本写整稿、自检、主动标风险 → ③ 如需音频，按 `手工跑通/` 的编号任务手工验证。不要去搭 App / RSS / 新闻流水线 / 分档系统。
- `Product_Methodology.md` — 「意义 → 机制 → 手动验证 → 才是系统」的打法；当前正处在"手动验证"这一步。

## 写任何稿子或内容时必须守的硬约束

这些是 PRD「产品宪法」，违反任何一条都会让产品的核心机制失效——比文风偏好重要得多：

1. **教育必须隐身。** 禁止"今天告诉我们……""所以我们要……""小朋友应该……"。绝不说教、不下结论。
2. **问题比答案更重要。** 提出值得想的开放问题、不收口；不提供标准答案。
3. **爸爸不是老师，是和孩子一起探索的人。** 给 Narrative 的硬尺子：**爸爸的台词里，问句要多于结论。**
4. **首先要有趣。** 孩子忍着听完的稿子，再有营养也是失败的。
5. **北极星（思维方式/性格的那些名词）永远不许出现在稿子里，也不许照着它写。** 它只用于自检稿子够不够深，必须藏在五条机制里自然发生。
6. **一期 = 一条主题新闻**深聊 10–15 分钟，不是多条速览。
7. **AI 不决定孩子听什么，家长决定。** AI 只产出 3 个候选（标 1 个推荐）+ 理由，拍板的永远是人。

写稿的体例、长度、年龄分档讲法、自检清单，以 `Writing_Kit.md` 第五~七节为准。

## 编辑文档时的约定

- 文档带版本号与日期（如 `PRD_v0.6`、`v0.1`），并在文件顶部有「更新记录/版本」说明。做实质性修订时，更新版本号、日期，并在更新记录里写清"改了什么、为什么"——参照 PRD v0.6 更新记录的写法。
- 术语保持一致：核心机制、产品宪法、北极星、红线/敏感/默认三层、家长档 × 年龄档、引擎五层（Content Intelligence / Cognitive / Narrative / Family Personalization / Voice Performance）、常青稿、单主题。
- 区分"已定设定"（不轻易动）与「开放问题」（PRD 第十五节列出的待决项，如正式命名、是否拆到 3 档、速览尾巴）。改前者要谨慎并说明理由。
- 全文中文。代号「喂，今天」，正式名待定。
