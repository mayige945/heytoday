# MiniMax 地球昆虫可能有两千万种短样记录 2026-07-08

## 目标

扩大新闻源后，按 `prompt v0.9` 和 `播客边界与证据锚点_v0.2` 跑一条更偏儿童自然科普的国际新闻，先生成短样验证：

1. 常规入口 `今天这期...` 加数字反差是否自然，第一声是否稳定。
2. `捕虫帐篷` 作为中等锚点是否清楚但不过度。
3. 贴纸类比是否能让孩子听懂“怎么估没抓到的部分”。
4. `1400 万到 2000 万` 数字锚点是否听得清，是否过载。
5. 直接出口 `今天这期新闻播客就到这里` 是否明确释放注意力。

## 输入

| 文件 | 用途 |
|---|---|
| `稿子/2026-07-08_地球昆虫可能有两千万种_小学档.md` | 完整文字稿 |
| `稿子/2026-07-08_地球昆虫可能有两千万种_minimax导演稿_v1.md` | 完整 MiniMax 导演稿 |
| `稿子/2026-07-08_地球昆虫可能有两千万种_minimax短样_E_v1.md` | 从完整导演稿抽出的短测稿 |

## 生成命令

```powershell
$env:HEYTODAY_TTS_BACKEND = "minimax"
$env:MINIMAX_REQUEST_INTERVAL_SEC = "2.2"
D:/project-script/VoxCPM/.venv/Scripts/python.exe tts/出音频.py 稿子/2026-07-08_地球昆虫可能有两千万种_minimax短样_E_v1.md
```

本次短样首次请求成功。

## 产物

| 产物 | 结果 |
|---|---|
| 音频 | `音频/2026-07-08_地球昆虫可能有两千万种_minimax短样_E_v1.minimax.mp3` |
| request txt | `音频/2026-07-08_地球昆虫可能有两千万种_minimax短样_E_v1.minimax.minimax-request.txt` |
| request json | `音频/2026-07-08_地球昆虫可能有两千万种_minimax短样_E_v1.minimax.minimax-request.json` |
| line parts | `音频/2026-07-08_地球昆虫可能有两千万种_minimax短样_E_v1.minimax.line_parts/` |

## 自动预检

| 项目 | 结果 |
|---|---|
| 时长 | 182.20s（约 3 分 02 秒） |
| 模式 | `line_concat` |
| turn 数 | 28 turn：爸爸 17 / 孩子 11 |
| endpoint | `https://api.minimaxi.com/v1/t2a_v2` |
| 首句 | `今天这期，先听一个有点挠人的数字：地球上的昆虫，可能不是六百万种，而是一千四百万到两千万种。<#0.25#>` |
| 末句 | `今天这期新闻播客就到这里。` |
| `voice_modify` | 无 |
| `timbre_weights` | 无 |
| calm emotion | 4 turn |
| 慢速 turn | 1 turn |
| 快速 turn | 1 turn |
| native tags | 无 `(gasps)/(laughs)/(sighs)` |
| 中文导演词外露 | request txt 未发现 `（温和）/（平静）/（慢）/（快）` |
| 强锚点残留 | request txt 未发现 `先记住/最后我们还要回到/先把耳朵` |
| 禁用说教句 | request txt 未发现 `今天我们来聊/今天告诉我们/所以我们要/小朋友应该/我们学到/你应该` |
| 生活场景动作 | request txt 未发现 `早餐桌/餐巾/筷子` |
| line parts | 30 个 mp3 文件 |

## 人工复听重点

1. 首句较长，听起来是否稳定自然，还是像在朗读标题和数字。
2. `六百万 -> 一千四百万到两千万 -> 一百万` 的开头数字链是否过载。
3. `捕虫帐篷` 是否能被记住，后面 `放回捕虫帐篷` 是否自然。
4. 贴纸类比是否真的讲清“没抓到的也能估”，还是仍显抽象。
5. 3 分 02 秒作为短样偏长；若其它听感通过，下一版只压短样，不先改完整稿。
6. 直接出口是否明确释放注意力，还是略硬。

## 当前判断

自动预检通过，可以进入成人复听。短样偏长是明确风险；如果复听主要问题只是时长，下一轮压成 90 秒以内的 `短样_E_v2`，保留完整导演稿 v1 不动。

---

## 第二轮：整期 v1 生成

### 生成命令

```powershell
$env:HEYTODAY_TTS_BACKEND = "minimax"
$env:MINIMAX_REQUEST_INTERVAL_SEC = "2.2"
D:/project-script/VoxCPM/.venv/Scripts/python.exe tts/出音频.py 稿子/2026-07-08_地球昆虫可能有两千万种_minimax导演稿_v1.md
```

使用 `MINIMAX_REQUEST_INTERVAL_SEC=2.2` 避免整期逐句请求触发 RPM 限流。本次整期首次请求成功。

### 产物

| 产物 | 结果 |
|---|---|
| 整期音频 | `音频/2026-07-08_地球昆虫可能有两千万种_minimax导演稿_v1.minimax.mp3` |
| request txt | `音频/2026-07-08_地球昆虫可能有两千万种_minimax导演稿_v1.minimax.minimax-request.txt` |
| request json | `音频/2026-07-08_地球昆虫可能有两千万种_minimax导演稿_v1.minimax.minimax-request.json` |
| line parts | `音频/2026-07-08_地球昆虫可能有两千万种_minimax导演稿_v1.minimax.line_parts/` |

### 自动预检

| 项目 | 结果 |
|---|---|
| 时长 | 446.47s（约 7 分 26 秒） |
| 模式 | `line_concat` |
| turn 数 | 83 turn：爸爸 57 / 孩子 26 |
| endpoint | `https://api.minimaxi.com/v1/t2a_v2` |
| 首句 | `今天这期，先听一个有点挠人的数字：地球上的昆虫，可能不是六百万种，而是一千四百万到两千万种。<#0.25#>` |
| 末句 | `今天这期新闻播客就到这里。` |
| `voice_modify` | 无 |
| `timbre_weights` | 无 |
| calm emotion | 9 turn |
| 慢速 turn | 2 turn |
| 快速 turn | 1 turn |
| native tags | 无 `(gasps)/(laughs)/(sighs)` |
| 中文导演词外露 | request txt 未发现 `（温和）/（平静）/（慢）/（快）` |
| 强锚点残留 | request txt 未发现 `先记住/最后我们还要回到/先把耳朵` |
| 禁用说教句 | request txt 未发现 `今天我们来聊/今天告诉我们/所以我们要/小朋友应该/我们学到/你应该` |
| 生活场景动作 | request txt 未发现 `早餐桌/餐巾/筷子` |
| line parts | 85 个 mp3 文件 |

### 人工复听重点

1. 首句数字链是否过密，尤其 `六百万 -> 一千四百万到两千万` 是否能一次听懂。
2. 7 分 26 秒的整期是否比短样自然；若仍觉得密，优先压缩数字，而不是加情绪控制。
3. `捕虫帐篷` 是否足够形成听觉画面，后面回收是否自然。
4. 贴纸类比是否真正解释了“没抓到的也能估”，还是听起来像另起一段。
5. `寄生蜂` 段是否过于不适；若孩子抗拒，下一版弱化成“这类小蜂”。
6. 爸爸 57 turn / 孩子 26 turn，是否有爸爸解释偏多的问题。
7. 直接出口是否明确释放注意力，还是略硬。

### 当前判断

整期 v1 自动预检通过，可以进入成人完整复听。通过后再给孩子听并记录真实反应。

---

## 第三轮：整期 v1 多音字修正版

### 触发反馈

复核文本后发现本期有多处 `数` 应读 `shu3`，如：

- `科学家能一只一只数吗？`
- `认真数了很多年`
- `还没数清楚`
- `大得数不完的世界`

但同稿也有 `数字`，其中 `数` 应读 `shu4`。因此不能全局把 `数` 改成 `shu3`。

### 本轮处理

在 `tts/出音频.py` 的 MiniMax payload 中新增短语级 `pronunciation_dict.tone`，不改正文台词：

| 短语 | 指定读音 |
|---|---|
| `一只只数` | `(yi4)(zhi1)(zhi1)(shu3)` |
| `一只一只数` | `(yi4)(zhi1)(yi4)(zhi1)(shu3)` |
| `数虫子` | `(shu3)(chong2)(zi5)` |
| `认真数` | `(ren4)(zhen1)(shu3)` |
| `数完` | `(shu3)(wan2)` |
| `数清楚` | `(shu3)(qing1)(chu3)` |
| `数不清` | `(shu3)(bu4)(qing1)` |
| `数不完` | `(shu3)(bu4)(wan2)` |

`数字` 不做覆盖，保留 `shu4`。

### 重跑命令

```powershell
$env:HEYTODAY_TTS_BACKEND = "minimax"
$env:MINIMAX_REQUEST_INTERVAL_SEC = "2.2"
D:/project-script/VoxCPM/.venv/Scripts/python.exe tts/出音频.py 稿子/2026-07-08_地球昆虫可能有两千万种_minimax导演稿_v1.md
```

本次重跑成功，覆盖同名整期音频。

### 自动预检

| 项目 | 结果 |
|---|---|
| 时长 | 447.60s（约 7 分 28 秒） |
| 模式 | `line_concat` |
| turn 数 | 83 turn：爸爸 57 / 孩子 26 |
| 首句 | `今天这期，先听一个有点挠人的数字：地球上的昆虫，可能不是六百万种，而是一千四百万到两千万种。<#0.25#>` |
| 末句 | `今天这期新闻播客就到这里。` |
| `voice_modify` | 无 |
| `timbre_weights` | 无 |
| pronunciation_dict | 已包含 `一只一只数/认真数/数清楚/数不完` 等 `shu3` 短语 |
| `数字` | 未覆盖，避免误读 |
| 中文导演词外露 | request txt 未发现 `（温和）/（平静）/（慢）/（快）` |
| native tags | 无 `(gasps)/(laughs)/(sighs)` |
| 禁用说教句 | request txt 未发现 `今天我们来聊/今天告诉我们/所以我们要/小朋友应该/我们学到/你应该` |
| 生活场景动作 | request txt 未发现 `早餐桌/餐巾/筷子` |

### 人工复听重点

1. `一只一只数吗` 是否读成 `shu3`。
2. `认真数了很多年` 是否读成 `shu3`。
3. `还没数清楚` 是否读成 `shu3`。
4. `大得数不完的世界` 是否读成 `shu3`。
5. `数字` 是否仍读成 `shu4`。

### 当前判断

多音字修正版自动预检通过，可以进入成人完整复听。当前可播放路径仍为 `音频/2026-07-08_地球昆虫可能有两千万种_minimax导演稿_v1.minimax.mp3`。

---

## 2026-07-10：SenseVoice 手工预检落地验证（短样 E v1）

对原短样的 28 个 `line_parts` 实际运行：

```powershell
$env:PYTHONUTF8="1"
D:/project-script/VoxCPM/.venv/Scripts/python.exe tts/评估音频.py 音频/2026-07-08_地球昆虫可能有两千万种_minimax短样_E_v1.minimax.minimax-request.json
```

产物已落在同目录：

- `2026-07-08_地球昆虫可能有两千万种_minimax短样_E_v1.minimax.emotion_raw.jsonl`
- `2026-07-08_地球昆虫可能有两千万种_minimax短样_E_v1.minimax.review_round1.csv`
- `2026-07-08_地球昆虫可能有两千万种_minimax短样_E_v1.minimax.fix_plan_round1.json`

SenseVoice 将中文数字规范化为阿拉伯数字；CER 已排除这一格式差异。本轮只筛出 1 个必须复听的 turn：第 9 句“捕虫网”回读成“补充啊”，`CER=0.15`。它进入成人复听和必要的发音/改写决策，不由模型单独判为失败。

本轮实际运行了 ASR 和情绪标签；自动自然度 MOS 尚未接入，仍按成人复听表判断“像真人 / 像对话”。
