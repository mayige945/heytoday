# 日志配置（plan §15）。文件运行日志保留 30 天，由 retention prune 清理。
# 绝不在日志中写入 API Key / Authorization / x-api-key / Cookie。

[logging]
level = "INFO"
file_level = "INFO"
console_level = "WARNING"
format = "%(asctime)s %(levelname)s [%(name)s] %(message)s"
datefmt = "%Y-%m-%dT%H:%M:%S%z"
